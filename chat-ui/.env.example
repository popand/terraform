# Chat UI Environment Variables
# Copy to .env and fill in after terraform apply

# API Gateway endpoint (from terraform output chat_ui_api_endpoint)
VITE_API_ENDPOINT=https://xxx.execute-api.us-east-2.amazonaws.com/prod/chat

# API Key (retrieve with: aws ssm get-parameter --name "/terraform-chat/api-key" --with-decryption --query Parameter.Value --output text)
VITE_API_KEY=your-api-key-here
